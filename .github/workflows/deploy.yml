name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Login to Docker Hub
      run: docker login -u ${{ secrets.DOCKER_USER }} -p ${{ secrets.DOCKER_PASS }}

    - name: Build frontend image
      run: docker build -t dharshanay/frontend ./frontend

    - name: Build backend image
      run: docker build -t dharshanay/backend ./backend

    - name: Push frontend image
      run: docker push dharshanay/frontend

    - name: Push backend image
      run: docker push dharshanay/backend